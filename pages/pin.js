import Layout from '../components/Layout';

const Pin = () => (
    <Layout>
        <div>
            <h1>Pinning Site </h1>
            <p>Harness the power of IPFS with all the additional functionality you need.</p>
        </div>
    </Layout>
);

export default Pin;